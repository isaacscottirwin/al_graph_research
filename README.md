# Active Learning on Signed Graphs

Research code for studying label propagation and active learning on signed graphs, including comparisons between edge removal and edge negation strategies.

## Structure

- `src/` — core implementation
- `tests/` — unit tests
- `notebooks/` — lightweight experiment drivers
- `configs/` — experiment configurations
- `results/` — outputs
- `docs/` — LaTeX writeup
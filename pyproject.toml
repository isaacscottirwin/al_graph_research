[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "al_graph_research"
version = "0.1.0"
dependencies = [
    "numpy",
    "scipy",
    "networkx",
    "graphlearning",
]

[tool.setuptools.packages.find]
where = ["src"]